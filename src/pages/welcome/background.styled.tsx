import styled from "styled-components"
import flexCenterMixin from "../../styles/mixins/flex-center.mixin";

const BackgroundStyled = styled.div`
  ${flexCenterMixin};
  
  width: 100vw;
  height: 100vh;
`

export default BackgroundStyled
